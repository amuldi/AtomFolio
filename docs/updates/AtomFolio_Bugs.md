# AtomFolio 버그/오류 기록

> [`README.md`](../../README.md)·[`AtomFolio_Updates.md`](AtomFolio_Updates.md)와는 다른 목적의
> 문서입니다. 업데이트 로그가 "무엇을 새로 만들었나"를 다룬다면, 이 문서는 **실제로 발견된
> 버그/오류만 따로 모아** 증상 → 원인 → 수정을 추적합니다. 날짜별 업데이트 로그에도 버그
> 수정이 섞여 있지만, 여기서는 그것만 뽑아 한 곳에서 훑어볼 수 있게 정리했습니다.
>
> **이 문서는 계속 갱신됩니다** — 앞으로 새로 발견되는 버그/오류도 여기에 추가합니다. 최신
> 항목이 맨 위에 옵니다. English version: [`AtomFolio_Bugs.en.md`](AtomFolio_Bugs.en.md)

## 상태 표기

- ✅ 수정됨 — 코드 수정 + 빌드/lint/테스트 통과 확인
- ⚠️ 수정됨, 실사용 미검증 — 코드는 고쳤고 자동 검증은 통과했지만, 실제 화면에서 직접
  조작해 확인하지는 못한 채 커밋됨 (해당 커밋 메시지에 그렇게 정직하게 남겨둠)
- 🗑️ 기능 제거 — 같은 문제가 반복돼서, 다시 고치는 대신 기능 자체를 없앰

---

## 2026-08-14

| 증상 | 원인 | 수정 | 상태 | 커밋 |
| --- | --- | --- | --- | --- |
| 해외 주식이 섞인 포트폴리오의 총 평가금액/총 매입금액/총 평가손익이 부정확함 | `portfolioAnalyticsSummary.js`의 `resolvePosition`이 종목별 통화를 전혀 구분하지 않고 buyAmount/marketValue를 그냥 합산 — USD 포지션의 원본 숫자가 KRW 포지션과 같은 단위인 것처럼 더해짐 | 종목마다 통화를 판별(실시간 시세 → 티커 형태 추정 → KRW 기본값)해서 기준 통화로 환산한 뒤 합산하도록 수정 (`src/utils/currency.js` 신설) | ✅ 수정됨 | `5f20c80` |
| 매수가 입력창에 통화 단위 표시가 전혀 없어 해외 종목 매수가를 USD/원 중 무엇으로 입력해야 하는지 알 수 없음 | UI에 단위 표시 자체가 없었음 | 입력창 라벨 옆에 해당 종목의 통화(USD/원) 배지 추가 | ✅ 수정됨 | `5f20c80` |
| 설정 화면에서 워크스페이스 ID 복사 버튼을 눌렀을 때 자동화 테스트 브라우저에서 아무 반응이 없음 | `navigator.clipboard.writeText()`가 클립보드 권한이 막힌 환경에서 resolve도 reject도 하지 않고 무한 대기 — 이 상태를 직접 재현해서 발견함(45초 타임아웃) | `Promise.race`로 800ms 타임아웃을 걸어, 응답이 없으면 `execCommand('copy')` 폴백으로 자동 전환하도록 수정 | ✅ 수정됨 | `5f20c80` |
| (사용자 제보) QQQM을 매수가 370,000·6주로 직접 추가하면 수익률이 -99%로 표시됨 | 매수가 입력창에 방금 추가한 "USD 달러" 배지가 있었지만 어디까지나 정보 표시일 뿐, 사용자가 원화(370,000원 감각)로 입력하는 걸 막지 못함 — 그 숫자가 그대로 USD로 취급되어 실제 시세($301.41)와 비교되며 (301.41-370000)/370000×100 ≈ -99.9%가 계산됨. 직접 재현해서 확인함 | 매수가 입력창을 "원/USD" 실제 토글로 변경 — 사용자가 고른 통화로 입력한 숫자를 종목의 실제 거래 통화로 환산한 뒤에만 수익률 계산과 저장에 사용하도록 수정(`resolveManualBuyPriceInNativeCurrency`). 환산 결과를 "≈ $262.01 (USD 환산)" 힌트로 바로 보여줌 | ✅ 수정됨 | `0acfddc` |

## 2026-08-13

| 증상 | 원인 | 수정 | 상태 | 커밋 |
| --- | --- | --- | --- | --- |
| 카테고리 연결선이 좌표 정렬을 다시 고친 뒤에도 여전히 이상한 위치에 그려짐 (사용자가 직접 캡처해서 재확인) | 좌표계 정렬 자체는 `800397a`에서 고쳤지만, 실제로는 테스트 포트폴리오 종목 다수가 같은 카테고리(대부분 ETF)에 속해 거의 모든 노드가 "같은 그룹"으로 잡혀 선택 노드에서 사방으로 선이 뻗어나가는 것처럼 보임 — 좌표는 맞아도 시각적으로 "이상한 선 뭉치"처럼 읽힘 | 같은 문제가 두 번째로 재현되어, 다시 고치는 대신 **연결선 렌더링 자체를 제거**(`.atom-group-links` SVG, `groupLinks` 계산 삭제). 같은 카테고리 노드를 흐려지지 않게 강조하는 하이라이트 효과는 그대로 유지 | 🗑️ 기능 제거 | `c0dac9d` |
| 카테고리로 연결된 종목끼리 이상한 위치에 선이 그려짐 (1차) | `.atom-group-links` SVG가 `.atom-materialize-wrapper`의 형제(sibling)로 배치되어, `.atom-visual-stage`의 전체 높이(하단 52px 여백 포함) 기준으로 `viewBox` 좌표가 매핑됨 — 실제 노드 좌표계와 어긋남 | SVG를 `.atom-materialize-wrapper` 안쪽으로 이동, 같은 박스·같은 materialize 스케일을 공유하도록 수정 — 그러나 위 항목처럼 근본적으로는 다시 재발함 | ✅ 좌표 문제 자체는 수정됨 (기능은 이후 제거) | `800397a` |
| 잠자기(잠금) 모드에서 원자 회전이 부자연스럽게 느껴짐 | 잠자기 상태의 창은 클릭 통과 전용이라 창 포커스를 절대 못 받음 → 회전 로직의 "engaged" 판정이 영구적으로 false → 항상 0.12배속(`IDLE_ROTATE_DISENGAGED_MULTIPLIER`)으로만 회전 | `state.sleeping`을 메인 프로세스가 브로드캐스트, 렌더러 rAF 루프에서 `sleepingRef`로 읽어 잠자기 중엔 항상 "engaged"(정상 속도)로 취급 | ✅ 수정됨 | `800397a` |
| 잠자기 토글이 설정 패널 안에 있어 발견하기 어려움 | 위치 선정 실수 — "위젯 켜짐" 여부와 붙어 있어야 자연스러운 토글인데 별개 설정 그룹에 넣음 | 트레이 아이콘 우클릭 메뉴로 이동, `원자 위젯 표시` 체크박스 옆에 배치 | ✅ 수정됨 | `0f7f165` |
| 앱을 완전히 새로 실행하면 위젯이 항상 화면 중앙에 뜨지 않음 (탭으로 껐다 켤 때만 중앙 정렬됨) | `createAtomWidget()`이 `atomWidget.showInactive()`를 직접 호출해, 중앙 정렬 로직이 들어있는 `setAtomWidgetVisible()`를 건너뜀 | `createAtomWidget()`이 `setAtomWidgetVisible(true)`를 거치도록 수정 | ✅ 수정됨 | `0f7f165` |
| 같은 카테고리 연결선이 점선이라 기존 결합선(bond-line)과 잘 안 어울림 | 디자인 선택 문제 (버그는 아니지만 실사용 피드백으로 발견) | `stroke-dasharray` 제거, 실선으로 변경 | ✅ 수정됨 | `095b4b2` |
| 종목 클릭 시 뜨는 정보박스가 원자와 시각적으로 겹침 | 정보박스에 배경이 없어 원자 위에 텍스트가 그대로 겹쳐 보임 | 1차: 반투명 검은 배경(스크림) 추가 → 사용자 피드백으로 반려("불투명한 검은박스는 없애") → 2차: `.atom-visual-stage`에 영구 하단 52px 여백을 둬 원자 자체를 살짝 작게/위로 그리고, 정보박스는 배경 없이 빈 공간에 뜨도록 수정 | ✅ 수정됨 | `f8eef2e` |
| 위젯을 다시 보일 때 마지막으로 끌어놓은 위치에 뜸 (매번 중앙에 뜨길 원함) | `setAtomWidgetVisible(true)`가 저장된 마지막 위치를 그대로 복원 | 보일 때마다 주 디스플레이 중앙 좌표로 강제 재배치하도록 변경 | ✅ 수정됨 | `f8eef2e` |
| 원자 위젯의 빈 공간(노드가 없는 영역)을 클릭해도 뒤에 있는 다른 앱이 클릭을 못 받음 | 투명 창인데도 `setIgnoreMouseEvents`를 한 번도 호출하지 않아, Electron이 창 전체를 불투명한 히트박스로 취급 | `document.elementFromPoint` 기반 히트테스트로 실제 상호작용 지점(`.node-hit`/`.center-hit`, 드래그 중인 배경)이 아니면 클릭을 뒤로 통과시키도록 수정 | ⚠️ 수정됨, 실사용 미검증 | `94797a2` |
| ⌘+드래그로 위젯을 빠르게 끌면 커서가 창을 앞질러 드래그가 "걸림" | 렌더러는 커서가 자기 창 위에 있을 때만 `pointermove`를 받는데, 빠른 드래그 시 커서가 창 경계를 벗어나면 이벤트가 끊김 | 렌더러의 델타 스트리밍 대신, 메인 프로세스가 `screen.getCursorScreenPoint()`를 16ms 주기로 직접 폴링하는 방식으로 교체 | ⚠️ 수정됨, 실사용 미검증 | `f8eef2e` |
| ⌘+드래그 중 ⌘ 키를 먼저 떼도 마우스 버튼을 뗄 때까지 창이 커서를 계속 따라다님 | `handleStagePointerDown`이 드래그 시작 시점에만 `metaKey`를 확인하고, 이후 `pointermove` 핸들러는 재확인하지 않음 | `handleMove`가 매 이벤트마다 `event.metaKey`를 확인해, ⌘가 풀리는 즉시 드래그 종료 | ⚠️ 수정됨, 실사용 미검증 | `d5cbba2` |

## 2026-08-12

| 증상 | 원인 | 수정 | 상태 | 커밋 |
| --- | --- | --- | --- | --- |
| 라이트 모드에서 원자가 거의 안 보임 | 후광(halo)과 알파 블렌딩이 비대칭으로 겹침, JS가 그리는 SVG 쪽 불투명도가 별도로 낮게 남아 있었음 | 여러 커밋에 걸쳐 후광/알파 수정, JS 쪽 SVG 불투명도 보정, 메인 라인/노드/라벨을 순수 검정으로 하드코딩, 그룹 블러·깊이-지터 제거, 스트로크/노드 두껍게 | ✅ 수정됨 (최종적으로는 라이트 모드 자체를 제거) | `8281107`, `2ea03b6`, `55e30c0`, `38303e7`, `e8b2a6d` |
| 특정 이름 패턴의 종목이 원자 화면에서 조용히 사라짐 | 종목 재검사(re-check) 단계의 이름 패턴 필터링 로직이 정상 종목까지 걸러냄 | 재검사 로직 수정 | ✅ 수정됨 | `ccc3ce6` |
| 수동으로 만든 포트폴리오 이름 뒤에 `.manual.csv`가 자동으로 붙음 | 파일 저장 로직이 수동 생성 여부와 무관하게 접미사를 항상 붙임 | 수동 생성 시 접미사를 붙이지 않도록 수정 | ✅ 수정됨 | `cd6e562` |

## 2026-08-10

| 증상 | 원인 | 수정 | 상태 | 커밋 |
| --- | --- | --- | --- | --- |
| macOS Spaces/전체화면 전환 중 원자 위젯이 사라짐 | 위젯 창의 `visibleOnAllWorkspaces`/레벨 설정이 Spaces 전환을 따라가지 못함 | 창 설정 수정 | ✅ 수정됨 | `bbb9bdd` |
| 단순히 마우스를 올리기만 해도(호버) 위젯이 움직임 | 이전 커밋에서 빠른 추가(quick-add) 숨김을 구현하며 호버 판정 조건이 과도하게 넓어짐 | 호버만으로는 이동이 시작되지 않도록 조건 원복 | ✅ 수정됨 | `0030179` |

## 2026-08-08

| 증상 | 원인 | 수정 | 상태 | 커밋 |
| --- | --- | --- | --- | --- |
| ⌘+드래그로 창을 옮기려 해도 창이 실제로는 움직이지 않음 | 이동 제스처와 회전 제스처 로직이 서로 충돌 | 두 제스처를 분리해 독립적으로 처리 | ✅ 수정됨 | `ab4da79` |

## 2026-08-05

| 증상 | 원인 | 수정 | 상태 | 커밋 |
| --- | --- | --- | --- | --- |
| 패키징된 메뉴바 앱이 조용히 크래시하며 창이 아예 안 뜸 | 패키징 환경에서 리소스 경로 해석이 개발 환경과 달라 초기화 중 예외 발생 (조용히 삼켜짐) | 경로 해석 수정 + 에러를 조용히 삼키지 않도록 처리 | ✅ 수정됨 | `03e6c77` |
| `PortfolioAllocationCard`가 `className` prop을 누락 | 컴포넌트가 받은 `className`을 루트 엘리먼트에 전달하지 않음 | prop을 루트에 전달하도록 수정 | ✅ 수정됨 | `ce71c43` |
| 날짜 기준(설정) 변경이 히트맵/뉴스 타임스탬프에 반영되지 않음 | 히트맵/뉴스 렌더링이 날짜 기준 설정을 구독하지 않고 고정값을 사용 | 설정값을 구독하도록 수정 | ✅ 수정됨 | `56a1873` |

## 2026-08-03

| 증상 | 원인 | 수정 | 상태 | 커밋 |
| --- | --- | --- | --- | --- |
| 로컬 포트폴리오 저장이 동시 쓰기로 유실될 수 있음 | 저장 함수가 직렬화 없이 병렬 호출을 허용 | 쓰기 큐를 도입해 직렬화 | ✅ 수정됨 | `bd7ea3f` |
| AI 요약의 위험도/데이터 품질 리스트 항목에 `font-family`가 적용 안 됨 | CSS 선택자가 해당 리스트 항목을 놓침 | 선택자 수정 | ✅ 수정됨 | `8406bc9` |

## 2026-05-23

| 증상 | 원인 | 수정 | 상태 | 커밋 |
| --- | --- | --- | --- | --- |
| 임포트한 포트폴리오의 티커가 잘못 파싱됨 | CSV 티커 컬럼 파싱 로직이 특정 포맷을 처리 못함 | 파싱 로직 보강 | ✅ 수정됨 | `a2d40a8` |

## 2026-05-08

| 증상 | 원인 | 수정 | 상태 | 커밋 |
| --- | --- | --- | --- | --- |
| 플로팅 툴 독(dock)이 예상대로 상호작용하지 않음 | 이벤트 핸들러 바인딩 문제 | 핸들러 수정 | ✅ 수정됨 | `897279d` |

---

## 운영/작업 환경 메모

코드 버그는 아니지만, 재발 방지를 위해 기록해 둡니다.

- **Electron 빌드가 엉뚱한 서명 인증서를 자동으로 집어감**: `npm run build:mac`이 키체인에 있던
  무관한 개인 Apple Development 인증서를 자동 선택해 서명함. `CSC_IDENTITY_AUTO_DISCOVERY=false`로
  자동 탐색을 꺼서 해결(사용자의 "서명 없이 배포" 결정과 일치).
- **electron-builder `dir` 타겟은 배포 불가**: 원래 `mac.target`이 `dir`(압축 안 된 `.app` 폴더)라
  GitHub Release 첨부 파일로 쓸 수 없었음. `zip` 타겟으로 변경(`89cc1c3`).
- **`git push`가 거부됨(origin이 앞서 있음)**: 사용자가 GitHub 웹 UI에서 직접 만든 커밋 3개가
  origin에 먼저 있어 로컬 push가 거부됨. `git fetch` + `git merge`로 정리 후 재푸시.
- **자동화 스크린샷이 무관한 개인 정보를 반복적으로 포함**: 전체 화면 캡처 방식이 Notes/
  카카오톡/다른 프로젝트 창까지 같이 찍는 사고가 여러 번 있었음. 매번 즉시 삭제하고, 이후
  `System Events`로 관련 없는 앱을 잠깐 숨긴 뒤 특정 영역만 `screencapture -R`으로 캡처하는
  방식으로 전환. 최종적으로는 메뉴바 앱 스크린샷은 사용자가 직접 촬영하는 것으로 정리됨.
- **프로덕션 사이트(`atomfolio.vercel.app`)가 80일간 재배포 안 됨 (2026-08-14 발견)**: Vercel
  프로젝트가 GitHub 저장소와 Git 연동이 안 되어 있어서, `main`에 푸시해도 자동 배포가 걸리지
  않음 — 마지막 프로덕션 배포가 5월 26일에서 멈춰 있었고, 그 사이의 7월 11일~8월 13일 커밋
  (WebGL 전환, 메뉴바 앱, Clerk 인증, KIS 시세, 명령 팔레트 등 사실상 이 changelog의 대부분)이
  실제 사이트에는 하나도 반영되지 않은 상태였음. `npx vercel --prod`로 최신 `main`을 수동
  배포하고, `vercel alias set`으로 `atomfolio.vercel.app`을 새 배포에 재연결해 해결. 재발
  방지를 위해 Vercel Git 연동(`vercel git connect`) 여부를 사용자와 논의 필요 — 아직 연결 안 됨.
  참고로 `/api/health` 확인 결과 `portfolioStore.driver: "memory"`, `databaseConfigured: false`로
  나와, 프로덕션이 Postgres(Neon) 없이 메모리 저장소로 돌고 있는 것도 함께 확인됨(별도 이슈).

## 참고

- 앱 자체에 대한 설명은 [`README.md`](../../README.md)를 참고하세요.
- 날짜별 기능 변경 이력은 [`AtomFolio_Updates.md`](AtomFolio_Updates.md)를 참고하세요.
- 이 문서의 English 버전: [`AtomFolio_Bugs.en.md`](AtomFolio_Bugs.en.md)
